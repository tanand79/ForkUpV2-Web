/**
 * Optional: copy to runtime-config.js on a static host only if you need to
 * override NEXT_PUBLIC_API_URL without rebuilding. Prefer Amplify / env instead.
 */
window.__FORKUP__ = window.__FORKUP__ || {};
// window.__FORKUP__.apiUrl = "http://54.242.154.3:3001";
